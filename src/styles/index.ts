import { createStitches } from '@stitches/react';

export const {
  config,
  styled,
  createTheme,
  css,
  getCssText,
  globalCss,
  keyframes,
  theme,
} = createStitches({
  theme: {
    colors: {
      main: '#8257e6',
    },
  },
});
